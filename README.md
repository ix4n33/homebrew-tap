A custom homebrew tap, for personal use only.

## Usage

```bash
brew tap ix4n33/tap
```

## Editing

Edit `sources.toml` to add or update casks and formulae. The Ruby files under
`Casks/` and `Formula/` are generated by:

```bash
python3 scripts/generate.py
```
